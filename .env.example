# Kindred App Configuration
# Copy this file to .env and edit the values as needed

# === Database Configuration ===
# For local PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kindred
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=kindred
PGHOST=localhost
PGPORT=5432

# === Authentication ===
# Generate a secure secret with: openssl rand -hex 32
AUTH_SECRET=your_auth_secret_here

# For Google Authentication (optional)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# === Application Settings ===
# Environment - Keep as development for local development
NODE_ENV=development

# Port - The port the server will listen on
PORT=5000

# Session Secret - Generate a secure secret with: openssl rand -hex 32
SESSION_SECRET=your_session_secret_here

# === Logging Configuration ===
# Log level: debug, info, warn, error
LOG_LEVEL=info

# === Server Configuration ===
# Base URL - The URL where the application is hosted
# For local development:
BASE_URL=http://localhost:5000
# For production:
# BASE_URL=https://www.getkindred.app

# === Feature Flags ===
# Enable/disable features
ENABLE_WEBSOCKETS=true
ENABLE_WEBRTC=true

# === PostHog Analytics (optional) ===
# VITE_POSTHOG_API_KEY=your_posthog_api_key
# VITE_POSTHOG_HOST=https://app.posthog.com